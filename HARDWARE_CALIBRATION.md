# 硬件校正系数说明

**版本**: V1.0.4  
**日期**: 2025-11-15  
**校正系数**: 2.248

---

## 🔍 问题发现

### 测量数据对比

| 测量方式 | 数值 | 说明 |
|---------|------|------|
| **万用表实测PA1** | 2455mV | 真实电压 |
| **ADC软件计算** | 1092mV | 未校正值 |
| **差异比例** | 2.248倍 | 硬件系数 |

### ADC原始数据

```
VREFINT ADC读数: 1500
PA1 ADC平均值:   1349
计算的VDDA:      3.312V
```

### 计算验证

```
ADC计算电压 = 1349 / 4095 × 3.312V = 1.092V ✅ 软件计算正确

实际电压 = 2.455V (万用表实测)

硬件系数 = 2455 / 1092 = 2.248
```

---

## ⚙️ 硬件原因分析

### 可能的硬件配置

#### 方案1：分压电路（最可能）

```
火焰传感器输出(5-12V) 
    ↓
   [R1] 10kΩ
    ↓
PA1引脚 -------→ STM32 ADC (0-3.3V)
    ↓
   [R2] 12kΩ
    ↓
   GND
   
分压比 = R2/(R1+R2) = 12/(10+12) = 0.545
实际系数 = 1/0.545 = 1.835

但测得2.248，可能分压比约 = 1/2.248 = 0.445
→ R1:R2 ≈ 10kΩ:8kΩ 或 12.5kΩ:10kΩ
```

#### 方案2：运放或电压跟随器

可能有信号调理电路，增益不是1:1。

#### 方案3：传感器自带分压

某些火焰传感器模块内置分压输出，适配3.3V系统。

---

## ✅ 软件校正实现

### 1. 添加校正系数

**文件**: `Src/config/systemConfig.h` 第75行

```c
/* Hardware calibration coefficient */
#define PA1_HARDWARE_SCALE_FACTOR   2.248f  /* 实测2455mV / ADC计算1092mV */
```

### 2. 应用校正

**文件**: `Src/main.c` 第530行

```c
static void taskAdcSample(uint32_t nowMs)
{
    // ... 读取ADC数据
    
    /* 计算基于VREFINT的电压 */
    vdda = adcCalculateVdda(vrefCountCycle);
    measValueRawMv = adcConvertToMillivolts(paAvg, vdda);
    
    /* ✅ 应用硬件校正系数 */
    measValueRawMv = measValueRawMv * PA1_HARDWARE_SCALE_FACTOR;
}
```

### 3. 调试模式也应用校正

**文件**: `Src/main.c` 第373行

```c
if (adcReadFlameSensorAverage(&paAvgRaw) == ADC_STATUS_OK) {
    float voltageBeforeScale = adcConvertToMillivolts(paAvgRaw, vddaCalc);
    voltageMv = (uint32_t)(voltageBeforeScale * PA1_HARDWARE_SCALE_FACTOR);
}
```

---

## 📊 校正效果验证

### 校正前

```
PA1实际电压: 2455mV (万用表)
ADC计算显示: 1092mV
误差: -55.5% ❌
```

### 校正后（预期）

```
PA1实际电压: 2455mV (万用表)
ADC计算显示: 1092mV × 2.248 = 2455mV
误差: 0% ✅
```

---

## 🧪 验证步骤

### 1. 编译烧录

```
Project → Build Target (F7)
烧录到MCU
```

### 2. 观察调试显示（ADC_RAW_DEBUG_MODE=1）

数码管循环显示：
```
步骤1: 1500  (VREFINT ADC)
步骤2: 1349  (PA1 ADC原始)
步骤3: 3312  (VDDA×1000)
步骤4: 2455  (校正后电压) ← 应该约等于万用表实测
```

### 3. 用万用表验证

- 测量PA1引脚电压
- 对比数码管显示的步骤4
- **两者应该接近** (误差<±50mV)

### 4. 切换到正常模式

在 `systemConfig.h` 中改：
```c
#define ADC_RAW_DEBUG_MODE   0u   // 关闭调试
```

重新编译，系统将显示校正后的火焰电压值。

---

## 🎯 校正系数调整方法

如果发现校正后仍有偏差，可以微调系数：

### 精确校正公式

```c
新系数 = 旧系数 × (万用表实测 / ADC显示值)
```

**示例**：
```
如果校正后显示2500mV，但万用表测得2455mV：
新系数 = 2.248 × (2455 / 2500) = 2.207
```

修改 `systemConfig.h`:
```c
#define PA1_HARDWARE_SCALE_FACTOR   2.207f  /* 微调后 */
```

---

## 📋 不同火焰状态下的校正验证

建议在多个电压点验证系数：

| 火焰状态 | 万用表(mV) | ADC显示(mV) | 系数 | 一致性 |
|---------|-----------|------------|------|--------|
| 无火焰 | 2455 | 2455 | 2.248 | ✅ |
| 微弱火焰 | 2000 | 2000 | 2.248 | ✅ |
| 强火焰 | 1500 | 1500 | 2.248 | ✅ |

**如果系数在不同电压下一致，说明是线性分压，一个系数即可校正全范围。**

---

## 🔄 VREFINT的作用（重要说明）

### 校正后的完整公式

```
最终电压 = (PA1_ADC / VREFINT_ADC) × VREFINT_CAL × 3000 / 4095 × 硬件系数

分解：
1) 用VREFINT动态校准VDDA → 消除温度/供电漂移
2) 用VDDA计算PA1电压 → 得到ADC参考下的电压
3) 乘以硬件系数 → 还原真实电压
```

### VREFINT的价值

即使有硬件系数，**VREFINT校准依然重要**：

```
假设温度升高，VDDA从3.3V降到3.2V：

❌ 不用VREFINT（固定VDDA=3.3V）：
   显示 = 1349 / 4095 × 3.3 × 2.248 = 2678mV
   误差 = (2678-2455)/2455 = +9% ❌

✅ 使用VREFINT（动态VDDA=3.2V）：
   显示 = 1349 / 4095 × 3.2 × 2.248 = 2597mV
   误差 = (2597-2455)/2455 = +5.8% ✅ 好一些
```

**但仍有误差！** 这说明**硬件分压电路本身也可能随温度漂移**。

---

## ⚠️ 重要发现

### 潜在问题

如果硬件分压电路的电阻也有温度系数，那么：

```
室温: 系数 = 2.248
高温: 系数可能变化 (电阻温漂)
```

### 建议

1. **测试不同温度下的系数一致性**
   - 室温25°C测一次
   - 高温65°C测一次
   - 看系数是否变化

2. **如果系数随温度变化**，可以添加温度补偿：
   ```c
   float scaleFactor = PA1_HARDWARE_SCALE_FACTOR + (tempC - 25) * 温度系数;
   ```

---

## 📝 总结

### 已完成

- ✅ 添加硬件校正系数 `PA1_HARDWARE_SCALE_FACTOR = 2.248`
- ✅ 在正常模式应用校正
- ✅ 在调试模式应用校正
- ✅ VREFINT动态校准依然保留（消除温度漂移）

### 预期效果

```
编译前: ADC显示 1092mV，实际 2455mV
编译后: ADC显示 2455mV，实际 2455mV ✅
```

### 下一步

1. **重新编译烧录**
2. **观察数码管显示步骤4**（应该显示约2455）
3. **用万用表验证**
4. **如有偏差，微调系数**

---

**校正系数已添加！重新编译后，显示值应该接近万用表实测值了！** 🎯


